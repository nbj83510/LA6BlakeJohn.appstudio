/*
let members = ["Mary","Sam","Joey","Paul","Mike","Char","Jenny","Kennedy","Kaden","Kurt","Anne"]
let i = 0

btnMember.onclick=function(){
  let memberNew = inptMember.value
  if (members.includes(memberNew))
    lblMember.value = memberNew + ", you are already a member."
  else {
    members.push(memberNew)
    lblMember.value = memberNew + ", you have been added to the membership roster."
}
}
*/